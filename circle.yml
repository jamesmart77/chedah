
dependencies:
  pre:
    - nvm install 9 && npm install
    - nvm use 9
  override:
    - nvm install 9 && npm install
    - nvm use 9

test:
  override:
    - nvm install stable && nvm alias default stable
    - npm test