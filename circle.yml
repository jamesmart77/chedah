
dependencies:
  pre:
    - nvm install 9 && npm install
    - nvm use 9
  override:
    - nvm install 9 && npm install
    - nvm use 9
    - npm install --save-dev babel-core
    - npm install --save-dev babel-preset-env

test:
  override: 
    - npm test